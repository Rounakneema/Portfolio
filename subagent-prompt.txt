You are a Principal Frontend Designer. Your task is to apply "enterprise-level" design polish and scroll animations to the pages in your assigned projects.

Assigned Projects: PROJECT1, PROJECT2

For EACH project, you must modify its `page.tsx` AND all subpages (`architecture/page.tsx`, `docs/page.tsx`, etc.).

1. Import Animations:
`import { ScrollReveal, StaggerContainer, StaggerItem } from '@/components/shared/ScrollReveal';`

2. Wrap content in animations:
Wrap major layout blocks (e.g., `<section>`, `<article>`, `<header>`, or large `<div>`s) in `<ScrollReveal>` so they elegantly fade and slide in as the user scrolls.
For lists, grids, or data tables, wrap the container in `<StaggerContainer>` and the items/rows in `<StaggerItem>`.

3. DO NOT add `'use client'` to any `page.tsx`. These pages export `metadata` and MUST remain Server Components. The `ScrollReveal` components are already client components and can be used directly.

4. Enhance Typography & UI:
- Add `tracking-tight` or `tracking-tighter` to large headings.
- Add `leading-relaxed` and `opacity-80` (or text-gray/slate) to paragraphs for readability.
- Make code blocks or cards stand out with glassmorphism (e.g., `bg-white/5 border border-white/10 backdrop-blur-md rounded-lg`).
- Add hover effects to interactive elements (e.g., `hover:-translate-y-1 hover:border-[BRAND_COLOR] transition-all duration-300`).

5. Keep the color palette! 
SortMail is light-themed (bg-gray-50, text-black). The others are dark-themed. Respect the project's accent colors (e.g. Amber for OSA, Fuchsia for MetroMind, Lime for Revealr).

Write back the updated files using the `write_to_file` or `replace_file_content` tools. Do not commit.
