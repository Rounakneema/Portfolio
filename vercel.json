{
    "functions": {
        "api/index.go": {
            "includeFiles": "api/static/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.go"
        }
    ],
    "buildCommand": "cd web && npm install && npm run build && cd .. && rm -rf api/static && mkdir -p api/static && cp -r web/out/* api/static/ && node bundler.js",
    "outputDirectory": "web/out"
}