{
    "functions": {
        "api/index.go": {
            "includeFiles": "api/static/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.go"
        }
    ],
    "buildCommand": "cd blog && npm install && npm run build && cd .. && rm -rf api/static && mkdir -p api/static && cp -r blog/out/* api/static/ && node bundler.js",
    "outputDirectory": "blog/out"
}